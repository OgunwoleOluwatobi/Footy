class Standing{
  int noo, winn, draws, losee, pointss;
  String name, image, no, win, draw, lose, points;
  Standing(this.no, this.name, this.win, this.draw, this.lose, this.points, this.image);
}